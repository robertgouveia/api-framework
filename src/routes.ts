import Router from "../lib/router/router";

/**
 * Adds a set of predefined routes to the given router instance.
 *
 * @param {Router} router - The router instance to which the routes will be added.
 * @return {void} Does not return a value.
 */
export default function AddRoutes(router: Router): void {

}